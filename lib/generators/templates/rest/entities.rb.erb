class <%= GrapeResource.class_name_prefix %>::<%= name.camelize.pluralize %>::Entities::<%= name.camelize.pluralize %> < Grape::Entity
  expose :id
remove<% @entities.each do |k, v| %>
  expose :<%= k %>
remove<% end %>
end
