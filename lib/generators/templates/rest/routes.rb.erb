module API
  module V1
    module <%= name.camelize.pluralize %>
      class Routes < Grape::API
        # Format response
        <%= GrapeResource.success_formatter %>
        <%= GrapeResource.error_formatter %>

        mount <%= GrapeResource.class_name_prefix %>::<%= name.camelize.pluralize %>::Resources::<%= name.camelize.pluralize %>
      end
    end
  end
end
